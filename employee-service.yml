server:
  port: 9000
spring:
  datasource:
    password: mohit
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
message:
  data: Hello from Employee Service spring cloud bus version
